<!DOCTYPE html>
<html>
  <head>
    <title>Stock</title>
    <meta name="viewport" content="width=device-width; initial-scale=1.0; maximum-scale=1.0; user-scalable=no target-density-Dpi=device-dpi">
    
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-md bg-dark navbar-dark sticky-top">
    <div class="container">
      <a class="navbar-brand" href=<%= root_path %>>股票網站</a>
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"> <span class="navbar-toggler-icon"></span> </button>
      <div class="collapse navbar-collapse row" id="navbarSupportedContent">
        <div class="mx-auto col-7">
          <ul class="navbar-nav nav-fill">
            <li class="nav-item">
              <a class="nav-link" href="#">特點</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">關於</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-white" href='#'>個股</a>
            </li>
          </ul>
        </div>
        <div class="col-md-5">
          <%= form_tag(info_index_path, method: :get, symbol: "search-form", class: "form-inline m-0 justify-content-end") do %>
            <%= text_field_tag(:search, params[:search], placeholder: "股票代碼", class: "form-control mr-1 col-5") %>
            <%= submit_tag("搜尋", class: "btn btn-primary") %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
  <%= yield %>
  <%= javascript_include_tag controller_name, "data-turbolinks-track" => true %>
  <%= stylesheet_link_tag controller_name, "data-turbolinks-track" => true %>

  </body>
</html>
